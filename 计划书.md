# 音乐播放器 计划书

> 目标：做一个从 https://www.yinyueku.cn 实时获取资源的听歌软件，尽量零依赖。

## 〇、实测发现（2026-09-16）

1. **站点 TLS 证书已过期**（TrustAsia DV，`notAfter 2025-08-12`，`www` 与裸域共用同一张）：
   浏览器/curl 直连 HTTPS 会以 `net::ERR_CERT_DATE_INVALID` 失败；HTTP 端口可正常返回数据
   （站点虽带 `Strict-Transport-Security` 头，但因 HTTPS 从未成功访问过，HSTS 并未写入生效）。
   → **对策 A（主）**：请求层内置 **HTTPS → HTTP 自动降级**（详见第四节），双击 HTML 即可用，无需任何服务；
   → **对策 B（备）**：随附零依赖本地代理 `server.py`（Python 标准库），转发时跳过证书校验，
   用于 HTTP 也被网络环境屏蔽的场景。
   两条对策都不改写业务姿势：站点证书修复后 HTTPS 首次尝试即成功，降级分支自动失效。
2. **sign 时效签名**：搜索结果携带 `sign`，请求 url/pic/lyric 必须带上，否则上游返回"签名错误！"。
   → **对策**：每次请求自动携带；一旦遇到"签名错误"，用"歌名+歌手"重新搜索换取新 sign 并重试一次
   （该机制同时保证阶段二歌单里的旧歌曲签名失效后可自愈）。

## 一、技术选型（零依赖方案）

**单文件 HTML 应用**（`音乐播放器.html`，双击即用）+ 可选本地代理（`server.py`）：

- **原生 HTML / CSS / JavaScript**，不引入任何框架、构建工具、npm 包
- **请求层双模式**（自动检测）：
  - `file://` 直开（默认）→ JSONP 直连站点，**HTTPS 失败自动降级 HTTP**
  - 经本地服务访问（http://127.0.0.1:8964）→ 同源 `fetch /api` 代理转发，绕开证书/网络限制
- **`<audio>` 标签**播放 mp3 直链（媒体播放不受跨域限制）
- **localStorage** 持久化本地数据（音量、播放模式，后续歌单）

前期已实测验证的接口（`https://www.yinyueku.cn/api.php`，GET/POST 均可，带 `callback=xxx` 走 JSONP）：

| 功能 | 参数 | 返回 |
|---|---|---|
| 搜索 | `types=search&count=30&pages=1&name=关键词` | 数组：`id / name / artist[] / album / pic_id / url_id / lyric_id / source / sign` |
| 播放直链 | `types=url&id=<id>&source=<源>&sign=<签名>` | `{url: "mp3直链"}`（带时效 vkey） |
| 封面 | `types=pic&id=<pic_id>&source&song=<id>&sign` | `{url: "封面图"}` |
| 歌词 | `types=lyric&id=<lyric_id>&source&song=<id>&sign` | `{lyric: "LRC文本"}` |

`source` 实测出现 `netease`（网易云）与 `tencent`（QQ音乐）双源互补。

## 二、分期计划

### 阶段一：在线搜索 + 听歌（本次实现）

1. **搜索**：关键词搜索，每页 30 条，支持"加载更多"翻页；显示歌名 / 歌手 / 专辑 / 来源角标
2. **播放**：点击歌曲即取直链播放；播放完自动下一首；播放失败自动跳过（连续 3 次后停止）
3. **播放控制**：播放/暂停、上一首/下一首、进度条拖动、音量（记忆）、播放模式（顺序/单曲循环/随机）
4. **歌词**：LRC 解析、逐行高亮滚动、点击歌词跳转进度
5. **封面**：当前歌曲封面展示，接入系统媒体键（MediaSession）

### 阶段二：歌单 + 下载（后续迭代）

1. **本地歌单**：新建/重命名/删除歌单，收藏歌曲入库，localStorage 持久化
2. **下载**：取直链后保存为 mp3 文件（浏览器下载能力范围内实现）
3. （可选）搜索历史、最近播放

## 三、风险与对策

- **直链有时效**：播放地址带 vkey，仅会话内缓存，过期即重新请求
- **部分源可能失败**：失败自动跳下一首；网易云/QQ 双源互补
- **站点接口若变更**：接口层集中在 JSONP 封装内，一处修改全局生效

## 四、里程碑

| 阶段 | 交付物 | 状态 |
|---|---|---|
| 阶段一 | `音乐播放器.html` + `server.py` + `启动播放器.bat`，可搜索、可播放、带歌词 | ✅ 完成（2026-09-16，端到端实测通过） |
| 阶段二 | 本地歌单（localStorage）+ 最近播放 + 下载 | ✅ 完成（2026-09-16，实测通过：收藏建单/持久化/假 sign 自愈/歌单播放/CDN 直链 blob 下载成功） |
| 阶段三（可选） | 搜索历史、歌单导入导出、批量下载 | 待议 |

## 五、使用方法

1. **双击 `音乐播放器.html`**，在搜索框输入歌名/歌手 → 点击歌曲即播（无需 Python、无需启动服务）
2. 歌曲行悬停出现 **☆ 收藏 / ⬇ 下载**；收藏时选择歌单或新建歌单
3. 左侧边栏：**最近播放**、**我的歌单**（悬停 ⋮ 可重命名/删除）；歌单页有"播放全部"
4. 底部播放条 ⬇ 可下载当前歌曲（浏览器直接保存，失败时自动开新标签手动 Ctrl+S）
5. 空格键播放/暂停；右下角"歌词"展开歌词面板

（可选）若 HTTP 也被网络环境屏蔽：双击 `启动播放器.bat` 或 `python server.py` 走本地代理中转，
关闭控制台窗口即停止服务。

## 六、HTTPS → HTTP 降级机制（2026-09-17）

**触发条件**（三者同时满足才降级）：
1. HTTPS 的 JSONP 请求确实失败（`onerror` 或超时，JSONP 无状态码可读）；
2. 页面自身**不是** HTTPS（`file://` / `http://`）——HTTPS 页面加载 HTTP 子资源会被浏览器
   按混合内容直接拦截，降级无意义（此情况会给出明确报错而非重试）；
3. 每次请求**仅重试一次**，不会在两种协议间反复横跳。

**失败判定**：`settled` 标志保证 `onerror`、超时、重复回调三者中只有一个能结算 Promise；
所有出口统一 `cleanup()`——清定时器、摘 `<script>` 节点、释放全局回调，避免悬挂请求与内存泄漏。
HTTPS 尝试超时设 8s（失败时尽快让位给降级），HTTP 尝试 15s。

**为何不写死 HTTP**：请求永远先发 HTTPS。站点证书一旦修复，HTTPS 首次尝试即成功并 `resolve`，
`catch` 分支根本不会执行——无需改动代码、无需清理开关。降级的唯一副作用是会话内首次降级时
弹一次「站点 HTTPS 证书已失效，本次改用 HTTP 连接」提示。
